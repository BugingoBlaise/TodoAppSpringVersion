package org.example.dao;


import org.example.entity.Todo;
import org.springframework.stereotype.Repository;

import java.util.ArrayList;
import java.util.List;


@Repository
public class TodoDao extends GenericDao<Todo> {
   /* TodoDao todoDao = new TodoDao();
    // Create a new Todo
    Todo newtodo = new Todo();
    newtodo.
    String resultCreate = todoDao.create(newtodo);

// Update a Todo
newTodo.setDescription("Milk, Bread, Eggs, and Orange Juice");
    String resultUpdate = todoDao.update(newTodo);

    // Find a Todo by ID
    Todo foundTodo = todoDao.findOne(Todo.class, newTodo.getId());

    // Delete a Todo
    String resultDelete = todoDao.delete(newTodo);

    // List all Todos
    List<Todo> allTodos = todoDao.findAll(Todo.class);


    */
}